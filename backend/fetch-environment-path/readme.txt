* Finds the path of Python, Java, C, and C++ compilers using platform-specific commands (where for Windows and which for Mac/Linux).
* Sends the paths to a server using an HTTP POST request via axios.
